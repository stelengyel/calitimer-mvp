---
phase: 04-pose-detection
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified: []
autonomous: false
requirements:
  - DETE-05

must_haves:
  truths:
    - "App builds clean on device (no Swift concurrency errors, no warnings)"
    - "Ember skeleton overlays the camera feed when a person is in frame during a live session"
    - "Skeleton toggle in SessionConfigSheet (gear icon) turns overlay on and off without affecting camera or controls"
    - "Skeleton toggle in SettingsView reflects and persists the same preference"
    - "Importing a video and playing it shows the ember skeleton tracking joint positions in real time"
    - "No perceptible frame drops or UI freezes during sustained live pose detection on device"
  artifacts: []
  key_links: []
---

<objective>
Build, run on device, and perform human verification of all Phase 4 success criteria: live skeleton, upload skeleton, toggle persistence, performance.

Purpose: Phase 4 is not complete until pose detection is confirmed working on real hardware — Vision performance on simulator does not reflect device behavior. This is the gate before Phase 5 can begin.
Output: Human-verified working pose detection in both live and upload modes.
</objective>

<execution_context>
@/Users/stelengyel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stelengyel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pose-detection/04-CONTEXT.md
@.planning/phases/04-pose-detection/04-02-SUMMARY.md
@.planning/phases/04-pose-detection/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Xcode project and build on device</name>
  <files></files>
  <action>
1. Regenerate the Xcode project to pick up the new `CaliTimer/Vision/` group added in Plan 01:
   - Run `xcodegen generate` in the project root to regenerate `CaliTimer.xcodeproj` from `project.yml`

2. Build for a connected device using XcodeBuildMCP:
   - Call `session_show_defaults` first to confirm active scheme and destination
   - Call `build_sim` (or `build_run_sim` if a device is the default destination) to compile and verify no errors

3. If build errors exist, fix them before proceeding to the checkpoint. Common issues to check:
   - Missing `@preconcurrency import AVFoundation` on files using AVFoundation types in nonisolated contexts
   - `CMSampleBufferCreateReadyWithImageBuffer` deprecation warning on iOS 16+ — if raised, use `CMSampleBufferCreateWithImageBuffer` instead (check Context7 for current signature)
   - `nonisolated(unsafe)` on `let` properties that are already safe — remove the annotation if the compiler complains

4. Verify the Xcode console shows Vision joint log output (`[Vision] detected N joints`) after the session starts and a person enters frame.
  </action>
  <verify>
    <automated>xcodegen generate 2>&1 | tail -5</automated>
  </verify>
  <done>Project builds clean (zero errors). Console shows [Vision] joint log lines during live session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 complete implementation:
    - VisionProcessor running VNDetectHumanBodyPoseRequest on every live camera frame
    - Ember skeleton overlay (8 joints + 8 bone lines) layered over live camera feed
    - Skeleton toggle in SessionConfigSheet (gear icon) and SettingsView
    - VisionProcessor running on imported video frames during playback
    - Ember skeleton overlay on VideoPlayerView in upload mode
    - Skeleton preference persists via UserDefaults across app launches
  </what-built>
  <how-to-verify>
    **Live camera verification:**
    1. Launch app on device. Tap "Start Session" from HomeView.
    2. Stand in front of the camera — the ember skeleton (orange joints + bone lines) should appear overlaid on your body within 1–2 seconds.
    3. Step out of frame — skeleton disappears cleanly.
    4. Tap the gear icon (bottom-right). The SessionConfigSheet should show a "Skeleton Overlay" toggle.
    5. Toggle it OFF — skeleton disappears. Camera feed, flip button, End Session button must still work normally.
    6. Toggle it ON — skeleton reappears. Dismiss sheet.
    7. Navigate to Settings (via drawer). Verify the "Skeleton Overlay" toggle shows the same state (ON) as set in the sheet.
    8. Toggle OFF in Settings. Return to a live session — skeleton should be OFF (preference persisted across views).

    **Upload mode verification:**
    9. Navigate to Upload mode (drawer → Upload). Import a video of a person from Photos.
    10. As the video plays, verify the ember skeleton tracks joint positions in real time on the video.
    11. Pause playback — skeleton should hold on the last detected frame.
    12. Scrub to a different position — skeleton should update to the new frame's joints.

    **Performance check:**
    13. Run a sustained 60-second live session with active pose detection. Observe camera feed — no stuttering, freezing, or dropped frames that make the app feel sluggish.
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass. Describe any failures for gap closure.</resume-signal>
</task>

</tasks>

<verification>
All four Phase 4 success criteria confirmed by human:
1. VNDetectHumanBodyPoseRequest runs on every live frame — skeleton renders
2. Skeleton toggle works independently without affecting other UI
3. VNDetectHumanBodyPoseRequest runs on imported video frames — skeleton renders on playback
4. No frame drops or UI freezes observed during sustained live pose estimation
</verification>

<success_criteria>
Human has verified all four Phase 4 success criteria on device. DETE-05 (Skeleton overlay rendered on camera feed, independently toggleable) is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pose-detection/04-04-SUMMARY.md`
</output>
