---
phase: 02-camera-setup-session-view
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: []
autonomous: false
requirements: [CAMR-01, CAMR-02, SESS-01, SESS-02]

must_haves:
  truths:
    - "Live camera feed appears immediately on session screen arrival"
    - "Camera flip works mid-session without freeze or crash"
    - "Start Session shows config sheet; Go navigates to live session"
    - "End Session returns to Home screen"
    - "Session record created in SwiftData when Go is tapped"
  artifacts:
    - path: "CaliTimer/Camera/CameraActor.swift"
      provides: "@globalActor declaration"
    - path: "CaliTimer/Camera/CameraManager.swift"
      provides: "AVCaptureSession lifecycle"
    - path: "CaliTimer/UI/LiveSession/LiveSessionView.swift"
      provides: "Full-bleed ZStack with overlaid controls"
    - path: "CaliTimer/UI/LiveSession/SessionConfigSheet.swift"
      provides: "Shared config sheet"
    - path: "CaliTimer/UI/Home/HomeView.swift"
      provides: "Sheet-gated session start"
  key_links:
    - from: "HomeView Start Session"
      to: "SessionConfigSheet → Session @Model insert → LiveSessionView"
      via: "sheet onConfirm closure"
      pattern: "modelContext\\.insert|navigate.*liveSession"
    - from: "LiveSessionView .task"
      to: "CameraManager.startSession()"
      via: "async actor hop"
      pattern: "\\.task.*startSession"
---

<objective>
Build and run Phase 2 on the iOS Simulator, then human-verify the complete camera + session flow against the Phase 2 success criteria.

Purpose: All automated work in Plans 01 and 02 is useless without a human confirming the camera feed actually appears, the camera flip works, and the session flow is navigable from a propped-phone perspective.

Output: Human approval or a list of issues for gap closure.
</objective>

<execution_context>
@/Users/stelengyel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stelengyel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-camera-setup-session-view/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and launch on iOS Simulator</name>
  <files></files>
  <action>
Use XcodeBuildMCP tools (per CLAUDE.md — never use xcodebuild directly for run operations):

1. Call `session_show_defaults` to confirm active scheme/simulator.
2. If defaults not set, call `session_set_defaults` with:
   - scheme: CaliTimer
   - simulatorName: iPhone 16 (or latest available)
3. Call `build_run_sim` to build and launch.
4. Capture any build errors or runtime warnings. Fix any errors before proceeding to checkpoint.

If build fails, diagnose and fix before advancing to the checkpoint task.
  </action>
  <verify>
    <automated>Build completes with 0 errors; app launches on simulator.</automated>
  </verify>
  <done>App is running on iOS Simulator — no build errors, no crash on launch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Phase 2 camera + session flow on simulator</name>
  <files></files>
  <action>
    Phase 2 complete camera + session flow built:
    - NSCameraUsageDescription in Info.plist (prevents crash)
    - CameraManager on CameraActor (Swift 6 safe AVFoundation)
    - Session @Model with startedAt/skill/targetDuration
    - Full-bleed ZStack LiveSessionView with overlaid controls
    - SessionConfigSheet shared component (Handstand + target time)
    - HomeView Start Session → sheet → Go → navigate flow
  </action>
  <verify>
    <automated>MISSING — human verification required for camera feed and UI flow</automated>
  </verify>
  <done>Human approves all 9 checklist items on iOS Simulator.</done>
  <how-to-verify>
    Test the following in order on the iOS Simulator:

    **1. Session Start Flow**
    - From Home screen, tap "Start Session"
    - Expected: Config sheet slides up (medium detent) showing "Session Setup", skill "Handstand", and a target hold time stepper
    - Adjust target hold time with + (increases by 5s) and − (decreases by 5s, stops at 0/None)
    - Tap "Go"
    - Expected: Sheet dismisses, app navigates to LiveSessionView

    **2. Live Camera Feed**
    - On LiveSessionView: camera feed should fill the screen edge-to-edge (no black bars on sides)
    - If running on Simulator: Simulator shows simulated camera output (a rotating cube or colored pattern) — this is correct for Simulator
    - Expected: No camera placeholder rectangle from Phase 1 — the real CameraPreviewView is active

    **3. Camera Permission** (Simulator)
    - On first run: system permission dialog appears asking for camera access
    - Tap "Allow" (or "OK")
    - Expected: Camera feed becomes active after permission granted

    **4. Controls Layout**
    - Confirm: camera flip button is in the TOP-RIGHT corner (arrow.triangle.2.circlepath.camera icon)
    - Confirm: End Session button is in the BOTTOM-LEFT (small text button with material background)
    - Confirm: Gear icon is in the BOTTOM-RIGHT

    **5. Camera Flip**
    - Tap the flip button (top-right)
    - Expected: Camera switches smoothly (Simulator shows different simulated feeds or rotates)
    - No freeze, no black screen, no crash

    **6. Mid-Session Config**
    - Tap the gear icon (bottom-right)
    - Expected: SessionConfigSheet slides up again
    - Dismiss without tapping Go — session continues
    - Expected: Camera feed still active, no navigation change

    **7. End Session**
    - Tap "End Session" (bottom-left)
    - Expected: App navigates back to Home screen

    **8. Session Persistence**
    - Start Session → config sheet → Go (creates a Session @Model record)
    - End Session
    - Repeat: Start Session → Go → End Session (creates a second Session record)
    - Expected: No crash on repeated create; SwiftData ModelContainer handles multiple Session records
    - (No history UI yet — verify via Xcode's Data Model Inspector or debug print if needed)

    **9. Orientation**
    - Rotate Simulator to landscape (Cmd+Left/Right arrow)
    - Expected: App stays in portrait — does NOT rotate (orientation lock from Phase 1 project.yml still active)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures for gap closure.</resume-signal>
</task>

</tasks>

<verification>
Human verification of:
1. Camera feed appears (no Phase 1 placeholder)
2. Camera flip works without crash
3. Start Session → sheet → Go flow creates Session and navigates
4. End Session returns to Home
5. Portrait orientation lock held
6. No crashes on repeated session start/end
</verification>

<success_criteria>
All 9 checklist items pass on iOS Simulator. Human types "approved". Phase 2 complete.

Phase 2 requirements satisfied:
- CAMR-01: Front/rear camera switch working
- CAMR-02: Portrait lock + overlaid controls = hands-free usable when propped
- SESS-01: Start Session (via sheet Go) and End Session (button) functional
- SESS-02: Session @Model created with startedAt on session start (holds will group to this in Phase 5+)
</success_criteria>

<output>
After completion, create `.planning/phases/02-camera-setup-session-view/02-03-SUMMARY.md`
</output>
